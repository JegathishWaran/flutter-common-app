import 'package:flutter/material.dart';

class HellowPage extends StatelessWidget {
  const HellowPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("hello page", style: TextStyle(fontSize: 54)));
  }
}
